# Hermes notes

- Respond in Polish unless the user asks otherwise.
- Before editing, inspect the repository docs and existing scripts.
- Keep changes minimal and aligned with the project's style.
- Never commit secrets, API keys, tokens, or `.env` files.
- Prefer the repo's documented install/test commands; if none exist, inspect package manifests or scripts first.
- After changes, verify with the project's native tests or build command when available.
